#include <stdio.h>
#include "myFile.h"
#define N 1
/* comment */
// comment
int i, j, arr[5];
int func(void);
int func(int i, char c);
int main(void) {
int i, j, arr[5];
/* comment */
// comment
if (a > b) {
a = b + c * d;
}
if (a != b) {
a = (b + c) * d;
}
else {
a += b;
}
while (a <= b) {
fun(x);
break;
}
return 0;
}
